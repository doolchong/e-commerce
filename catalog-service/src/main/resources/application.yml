server:
  port: 0

spring:
  application:
    name: catalog-service
  cloud:
    config:
      name: ecommerce-dev, catalog-service-dev
  config:
    import:
      - optional:configserver:${CONFIG_SERVER_URI}